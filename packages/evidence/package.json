{
  "name": "@geo-lens/evidence",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical evidence semantics for GeoLens",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
