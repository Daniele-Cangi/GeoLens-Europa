{
  "name": "geo-lens-eu",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web",
    "packages/evidence",
    "packages/providers",
    "packages/stormwater",
    "packages/proof-zero"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:refoundation": "npm run build",
    "dev": "node scripts/dev-proof-zero.mjs",
    "dev:api-web": "concurrently \"npm run dev --workspace=@geo-lens/api\" \"npm run dev --workspace=@geo-lens/web\"",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test:ts && npm run test:python",
    "test:ts": "npm run test --workspaces --if-present",
    "test:python": "npm run test:python:nasa && npm run test:python:benchmark",
    "test:python:nasa": "cd nasa-precip-engine && python -m unittest discover -s tests -v",
    "test:python:benchmark": "python -m unittest discover -s tests/python -v",
    "test:live": "npm run build --workspace=@geo-lens/providers && node --test packages/providers/test/live-environmental.test.cjs && cd nasa-precip-engine && python -m unittest discover -s tests -p test_live_imerg.py -v",
    "test:live:proof-zero": "node --test apps/api/test/live-proof-zero-runtime.test.cjs",
    "verify:ground-truth": "npm run build --workspace=@geo-lens/evidence && node scripts/verify-ground-truth-artifacts.mjs",
    "materialize:emilia-inputs": "npm run build --workspace=@geo-lens/evidence && npm run build --workspace=@geo-lens/providers && node scripts/materialize-emilia-bounded-inputs.mjs",
    "materialize:emilia-xdbtr": "python scripts/materialize_emilia_xdbtr_masks.py",
    "verify:emilia-inputs": "npm run build --workspace=@geo-lens/evidence && npm run build --workspace=@geo-lens/providers && node scripts/verify-emilia-bounded-inputs.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "geotiff": "^2.1.4-beta.1",
    "proj4": "^2.21.0"
  }
}
